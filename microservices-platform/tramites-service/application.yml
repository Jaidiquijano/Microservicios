server:
  port: 8085

spring:
  application:
    name: tramites-service

  datasource:
    url: jdbc:postgresql://localhost:5436/tramites_db
    username: lumara
    password: lumara123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always