server:
  port: 8085

spring:
  application:
    name: mobile-bff

  cloud:
    gateway:
      routes:
        - id: dashboard
          uri: http://localhost:8081
          predicates:
            - Path=/mobile/dashboard
          filters:
            - RewritePath=/mobile/dashboard, /api/users/profile