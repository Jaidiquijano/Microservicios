server:
  port: 8083

spring:
  application:
    name: agenda-service

  datasource:
    url: jdbc:postgresql://localhost:5434/agenda_db
    username: lumara
    password: lumara123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
